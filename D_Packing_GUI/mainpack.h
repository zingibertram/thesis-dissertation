#ifndef MAINPACK_H
#define MAINPACK_H

#include "utils.h"

void mainPacking(char *filename = "data.in");

double packing(FigureList fs);
void lowBounds(FigureList fs);

#endif // MAINPACK_H
